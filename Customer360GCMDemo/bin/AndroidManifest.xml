<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:versionCode="1" android:versionName="1.0" package="com.customer360gcmdemo">

    <uses-sdk android:minSdkVersion="9" android:targetSdkVersion="21"/>
    
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    
    <!-- GCM connects to Google Services. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- GCM requires a Google account. -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>

    <!-- Keeps the processor from sleeping when a message is received. -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!--
     Creates a custom permission so only this app can receive its messages.
     NOTE: the permission *must* be called PACKAGE.permission.C2D_MESSAGE,
           where PACKAGE is the application's package name.
    -->
    <permission android:name="com.google.android.gcm.demo.app.permission.C2D_MESSAGE" android:protectionLevel="signature"/>
    <uses-permission android:name="com.google.android.gcm.demo.app.permission.C2D_MESSAGE"/>

    <!-- This app has permission to register and receive data message. -->
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE"/>

    <application android:allowBackup="true" android:icon="@drawable/ic_launcher" android:label="@string/app_name" android:theme="@style/AppTheme">
        
        <!-- Required for applications which use Google Play Services. -->
        <meta-data android:name="com.google.android.gms.version" android:value="@integer/google_play_services_version"/>
        
        <activity android:label="@string/app_name" android:name="com.customer360gcmdemo.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <!--
          WakefulBroadcastReceiver that will receive intents from GCM
          services and hand them to the custom IntentService.
          The com.google.android.c2dm.permission.SEND permission is necessary
          so only GCM services can send data messages for the app.
        -->
        <receiver android:name="com.customer360gcmdemo.GcmBroadcastReceiver" android:permission="com.google.android.c2dm.permission.SEND">
            <intent-filter>
                <!-- Receives the actual messages. -->
                <action android:name="com.google.android.c2dm.intent.RECEIVE"/>
                <category android:name="com.tiktik"/>
            </intent-filter>
        </receiver>
        <service android:name="com.customer360gcmdemo.GcmIntentService"/>

        <activity android:label="@string/app_name" android:name="cus360.inapp.helpdesk.app.ticket.createticket.ActivityCreateTicket">
        </activity>
        <activity android:label="@string/app_name" android:name="cus360.inapp.helpdesk.app.ticket.createticket.ActivityPreview" android:windowSoftInputMode="stateAlwaysHidden">
        </activity>
        <activity android:label="@string/app_name" android:name="cus360.inapp.helpdesk.app.ticket.ticketlisting.ActivityTicketListing" android:windowSoftInputMode="stateAlwaysHidden">
        </activity>
        <activity android:label="@string/app_name" android:name="cus360.inapp.helpdesk.app.ticket.ticketlisting.ActivityTicketDetailsConversation" android:windowSoftInputMode="stateAlwaysHidden">
        </activity>
        <activity android:label="@string/app_name" android:name="cus360.inapp.helpdesk.app.ticket.ticketlisting.gallery.ActivityPhotoGallery" android:windowSoftInputMode="stateAlwaysHidden">
        </activity>
        <activity android:label="@string/app_name" android:name="cus360.inapp.helpdesk.app.thankyou.ActivityThankYou" android:windowSoftInputMode="stateAlwaysHidden">
        </activity>
        
        <activity android:label="@string/app_name" android:name="cus360.inapp.chat.app.src.ChatFragmentActivity" android:screenOrientation="portrait" android:windowSoftInputMode="adjustResize">
        </activity>
        
        <service android:name="cus360.inapp.chat.service.XmppServerPing"/>
        <activity android:label="@string/app_name" android:name="com.customer360coresdk.MainActivity">
        </activity>
    </application>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.PARTIAL_WAKE_LOCK"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    
    <uses-feature android:name="android.hardware.camera"/>
    <uses-feature android:name="android.hardware.camera.autofocus"/>

</manifest>
